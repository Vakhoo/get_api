# this app fetches data from api and saves in database.

[comment]: <> (## To run migration run "php migrate" from project directory.)

## Available Scripts

In the project directory, you can run:

### `php migration.php`

Creates database and runs migration.
